// Enable prerendering for static pages (better performance & SEO)
export const prerender = 'auto';

// Enable SSR for SEO (search engines see full content)
export const ssr = true;

// Consistent URLs for SEO (no duplicate content)
export const trailingSlash = 'never';

// Enable CSR for interactivity after hydration
export const csr = true;
